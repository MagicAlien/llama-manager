{
  "name": "llama-manager",
  "version": "0.0.0",
  "private": true,
  "description": "T-000 bootstrap: the minimum that makes `npm run lint` and `npm run test` meaningful before T-001 replaces it with the real scaffold.",
  "scripts": {
    "lint": "node scripts/lint-empty.js",
    "test": "node --test src"
  }
}
