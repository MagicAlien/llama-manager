// T-000 bootstrap: this file exists so `node --test src` has a real
// directory to run against and exits zero with zero tests. T-001 replaces
// the entire src/ tree with the real React renderer.
export {};
